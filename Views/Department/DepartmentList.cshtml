@model List<Department>

<div class="pagetitle">
    <h1>Department</h1>
    <nav>
        <ol class="breadcrumb">
            <li class="breadcrumb-item"><a asp-controller="Home" asp-action="Index"><i class="fa-solid fa-house-chimney"></i> Home</a></li>
            <li class="breadcrumb-item active"><i class="fa-solid fa-building"></i> Department</li>
        </ol>
    </nav>
</div><!-- End Page Title -->

@if (TempData["SuccessMessage"] != null)
{
    <div class="alert alert-success alert-dismissible fade show" role="alert">
        <i class="bi bi-check-circle-fill me-1"></i> @TempData["SuccessMessage"]
        <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
    </div>
}
@if (TempData["ErrorMessage"] != null)
{
    <div class="alert alert-danger alert-dismissible fade show" role="alert">
        <i class="bi bi-exclamation-triangle-fill me-1"></i> @TempData["ErrorMessage"]
        <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
    </div>
}

<div class="m-2">
    <div class="bg-primary-subtle rounded row pt-2 pb-2 align-items-center">
        <div class="col-7"></div>
        <div class="col-5 text-end pe-4">
            <a asp-controller="Department" asp-action="DepartmentAddEdit" class="btn btn-primary me-2"><i class="bi bi-plus-square-fill"></i> Add Department</a>
        </div>
    </div>
</div>

<section class="section">
    <div class="row">
        <div class="col-lg-12">

            <div class="card">
                <div class="card-body">
                    <h5 class="card-title">Department List</h5>

                    <!-- Table with stripped rows -->
                    <table class="table datatable">
                        <thead>
                            <tr>
                                <th class="text-center">Department ID</th>
                                <th>Department Name</th>
                                <th class="text-center">Created</th>
                                <th class="text-center">Modified</th>
                                <th class="text-center">Action</th>
                            </tr>
                        </thead>
                        <tbody>
                            @foreach (var item in Model)
                            {
                                <tr>
                                    <td class="text-center">@item.DepartmentID</td>
                                    <td>@item.DepartmentName</td>
                                    <td class="text-center">@item.Created.ToString("yyyy-MM-dd HH:mm")</td>
                                    <td class="text-center">@item.Modified.ToString("yyyy-MM-dd HH:mm")</td>
                                    <td class="text-center">
                                        <a asp-controller="Department" asp-action="DepartmentAddEdit" asp-route-id="@item.DepartmentID" class="text-warning me-3">
                                            <i class="bi bi-pencil-square fs-5"></i>
                                        </a>
                                        <a asp-controller="Department" asp-action="Delete" asp-route-id="@item.DepartmentID" class="text-danger"
                                           onclick="return confirm('Are you sure you want to delete this department?')">
                                            <i class="bi bi-trash fs-5"></i>
                                        </a>
                                    </td>
                                </tr>
                            }
                        </tbody>
                    </table>
                    <!-- End Table with stripped rows -->

                </div>
            </div>

        </div>
    </div>
</section>
